# Credentials
.env
agents/manao/pipeline/.env

# Product data (ข้อมูลสินค้า + รูป)
products/

# News articles (manao pipeline)
agents/manao/pipeline/news/

# Lock files (bot single-instance PID locks — must never be committed)
.approval-bot.lock
agent-status.json
.auth-sessions.json
**/*.lock

# Financial / PII data — Agent มะยม (สลิปโอนเงินจริง เลขบัญชี จำนวนเงิน)
# ห้าม commit ขึ้น public repo เด็ดขาด (daily-pr.js ทำ git add -A)
agents/*/transactions/
agents/*/slips/
agents/mayom/users.json
agents/mayom/index.json
agents/mayom/last-group.json

# Internal-only affiliate tracking (ไม่เปิด public)
tracking.xlsx

# Deploy state backups (สร้างโดย lib/ci/deploy-runner.js สเต็ป 3)
backups/

# Video temp files
*/_temp_video/
*.mp4

# Node
node_modules/
npm-debug.log*

# Logs
*.log

# OS
.DS_Store
Thumbs.db
